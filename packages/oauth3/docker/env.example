# ============================================================
# OAuth3 Agent Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values
# For detailed provider setup instructions, see docs/PROVIDER_SETUP.md
# ============================================================

# ============ Core Configuration ============
# Port for the OAuth3 agent HTTP server
OAUTH3_PORT=4200

# Chain ID: 420691 (localnet), 420690 (testnet), 420692 (mainnet)
CHAIN_ID=420691

# RPC endpoint for your network
JEJU_RPC_URL=http://localhost:9545

# ============ TEE Configuration ============
# TEE mode: simulated (dev), dstack (Intel TDX), phala (Phala Network)
TEE_MODE=simulated

# Private key for the TEE agent (only for simulated mode)
# In production, keys are generated inside the TEE
# TEE_PRIVATE_KEY=0x...

# ============ MPC Configuration ============
# Enable FROST threshold MPC signing
MPC_ENABLED=false

# Number of parties required to sign (threshold)
MPC_THRESHOLD=2

# Total number of MPC parties
MPC_TOTAL_PARTIES=3

# ============ Storage (IPFS) ============
# IPFS API endpoint for storing sessions and credentials
IPFS_API_ENDPOINT=http://localhost:4030/storage/api/v0

# IPFS gateway for retrieving data
IPFS_GATEWAY_ENDPOINT=http://localhost:4030/storage/ipfs

# ============ Google OAuth ============
# Create at: https://console.cloud.google.com/apis/credentials
# Callback URL: https://your-app.com/auth/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============ GitHub OAuth ============
# Create at: https://github.com/settings/developers
# Callback URL: https://your-app.com/auth/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ============ Twitter/X OAuth ============
# Create at: https://developer.twitter.com/
# Callback URL: https://your-app.com/auth/callback
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# ============ Discord OAuth ============
# Create at: https://discord.com/developers/applications
# Callback URL: https://your-app.com/auth/callback
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# ============ Apple Sign-In ============
# Requires Apple Developer Program ($99/year)
# See docs/PROVIDER_SETUP.md for detailed setup instructions
# Client ID is your Services ID (e.g., com.yourcompany.app.oauth3)
APPLE_CLIENT_ID=
# Client secret is a JWT signed with your key - must be regenerated every 6 months
APPLE_CLIENT_SECRET=
APPLE_TEAM_ID=
APPLE_KEY_ID=

# ============ Farcaster ============
# No registration required for basic auth
# These are optional, only needed for posting casts or bot actions
FARCASTER_APP_NAME=Your App
FARCASTER_APP_DOMAIN=your-app.com
# FARCASTER_FID=12345
# FARCASTER_SIGNER_KEY=0x...
# NEYNAR_API_KEY=

# ============ Email Authentication ============
# SMTP configuration for sending magic links and OTP codes
# Popular providers: SendGrid, Resend, AWS SES, Postmark, Mailgun

# SendGrid example:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Sender information
EMAIL_FROM_ADDRESS=auth@your-app.com
EMAIL_FROM_NAME=Your App

# Magic link configuration
MAGIC_LINK_BASE_URL=https://your-app.com/auth/verify
MAGIC_LINK_EXPIRY_MINUTES=15

# OTP configuration
OTP_EXPIRY_MINUTES=10
OTP_LENGTH=6

# ============ Phone/SMS Authentication ============
# SMS provider: twilio or aws-sns
SMS_PROVIDER=twilio

# Twilio configuration (https://www.twilio.com/console)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=+15551234567

# AWS SNS configuration (alternative to Twilio)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# ============ Development Settings ============
# Enable debug logging
# DEBUG=oauth3:*

# Skip actual email/SMS sending in development (logs to console instead)
# NODE_ENV=development
