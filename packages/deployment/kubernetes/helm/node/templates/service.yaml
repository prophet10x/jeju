apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeju-node.fullname" . }}
  labels:
    {{- include "jeju-node.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "jeju-node.selectorLabels" . | nindent 4 }}

