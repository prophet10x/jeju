{
  "name": "@jeju/git",
  "version": "0.1.0",
  "description": "JejuGit - Decentralized Git Server",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "lint": "eslint src"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@irys/sdk": "^0.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
